Undisliked a post

<% if activity.trackable %>
  <%= link_to activity.trackable.content, activity.trackable %> <% if activity.created_at > Time.now.beginning_of_day %> 
    <%="#{time_ago_in_words(activity.created_at)} ago"%> 
<% else %>
   on  <%= activity.created_at.strftime("%m/%d/%Y %I:%M%p") %>
<% end %> 
<% else %>
  which has since been removed
<% end %>