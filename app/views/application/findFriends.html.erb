<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <a href="/user/<%=user.username %>"><%= image_tag(user.avatar.url, class: "img-circle", width: "40") %></a>
    <%= user.name%> 
  <% end %>
</ul>